export function DesktopRecommendedBanner() {
  return (
    <div className="border-b border-[var(--accent-primary)]/20 bg-[var(--accent-primary)]/10 px-4 py-2 text-center text-xs text-[var(--accent-primary)] sm:hidden">
      MyGPT needs Chrome&apos;s on-device AI, which currently only runs on
      desktop or laptop Chrome — it isn&apos;t available on mobile browsers
      yet. Open this page on a desktop Chrome to chat.
    </div>
  );
}
